"""SAM (Segment Anything) wrapper.

Stage 1 stub. Stage 2 ports / cleans the prior ``utils/sam_segmentation.py``.
"""

from __future__ import annotations


def get_instance_masks(image, *, model: str = "vit_h", points_per_side: int = 32):
    raise NotImplementedError("Stage 2")
